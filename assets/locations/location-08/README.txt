Location 08 — drop your assets here:
  background.jpg   (or .png) — full-screen stage background (1920x1080 ideal)
  music.mp3        — looping background music for sets played at this location
Filenames are referenced by src/locations.js (BG_FILE / MUSIC_FILE).
